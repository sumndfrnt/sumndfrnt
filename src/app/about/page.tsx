import { AboutSection } from "@/components/about-section";

export const metadata = {
  title: "About — SUM'N DFRNT",
  description: "From the culture. For what's next.",
};

export default function AboutPage() {
  return (
    <div className="pt-20">
      <AboutSection />
    </div>
  );
}
